@package lang

let trace = func(message) {
    intrinsic trace (message)
}
@export trace

let sleep = func(duration) {
    let fut = future
    intrinsic notify_after (fut, duration)
    fut
}
@export sleep